source 'https://rubygems.org'

gem 'rails', '3.2.13'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'mysql2', '0.3.13'
#gem "mongoid", "~> 3.0.0"

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'

#gem install nokogiri -v 1.5.10 -- --with-xml2-dir=/usr --with-xslt-dir=/opt/local --with-iconv-dir=/opt/local

#social stream GEM
#http://social-stream.dit.upm.es/started/#more
#gem 'social_stream', :git => 'git@github.com:ging/social_stream.git', :branch => 'master'
gem 'social_stream-base', '2.2.0'
gem 'social_stream-documents', '2.2.0'
gem 'social_stream-events', '2.2.0'
gem 'social_stream-linkser', '2.2.0'
gem 'social_stream-oauth2_server', '2.2.0'
gem 'social_stream-ostatus', '2.2.0'
gem 'social_stream-presence', '2.2.0'